sudo apt-get update
sudo apt-get install picocom python-setuptools arduino -y
sudo easy_install pip
git clone git://github.com/amperka/ino.git
sudo easy_install ino
sudo rm -rf ino

#sudo adduser "username" dialout
